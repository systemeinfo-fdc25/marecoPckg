% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calcul_iecmar.R
\name{build_gpkg_name}
\alias{build_gpkg_name}
\title{Build GeoPackage filename from processing parameters}
\usage{
build_gpkg_name(
  version,
  departement,
  use_OS_for_reseaux,
  use_RD_for_reseaux,
  buffer_size_for_reseaux,
  prefix = "resultats",
  dir = "./output"
)
}
\arguments{
\item{version}{Integer. Processing version used in `process_all()`.}

\item{departement}{Integer or character. Department identifier.}

\item{use_OS_for_reseaux}{Logical. Whether OS networks are used.}

\item{use_RD_for_reseaux}{Logical. Whether RD networks are used.}

\item{buffer_size_for_reseaux}{Numeric. Buffer size used for networks (in meters).}

\item{prefix}{Character. File name prefix. Default is `"resultats"`.}

\item{dir}{Character. Output directory path. Default is `"./output"`.}
}
\value{
Character string. Full path to the generated GeoPackage file.
}
\description{
Constructs a standardized GeoPackage (.gpkg) file name based on
the parameters used in the `process_all()` workflow. This ensures
reproducible and traceable output file naming.
}
\details{
The file name is constructed using a standardized pattern:
`prefix_v{version}_{departement}_{OS/RD flags}_{buffer}m.gpkg`.

OS and RD flags are encoded as:
\itemize{
  \item `"OS"` / `"noOS"` depending on `use_OS_for_reseaux`
  \item `"RD"` / `"noRD"` depending on `use_RD_for_reseaux`
}

This function does not check file existence or create directories.
}
\examples{
build_gpkg_name(
  version = 4,
  departement = 25,
  use_OS_for_reseaux = FALSE,
  use_RD_for_reseaux = FALSE,
  buffer_size_for_reseaux = 750
)

}
